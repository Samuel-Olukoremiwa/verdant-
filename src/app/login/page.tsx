'use client'
import Link from 'next/link'
import { FormEvent, useState } from 'react'
import { useRouter } from 'next/navigation'
import { createClient } from '@/lib/supabase/client'
export default function LoginPage() { const router = useRouter(); const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState<string|null>(null); const [loading,setLoading]=useState(false); async function submit(e: FormEvent<HTMLFormElement>) { e.preventDefault(); setLoading(true); setError(null); const {error}=await createClient().auth.signInWithPassword({email,password}); if(error){setError(error.message);setLoading(false);return} router.replace('/auth/redirect');router.refresh() } return <main className="login-page"><section className="login-panel"><Link href="/" className="brand login-brand"><span className="brand-mark">V</span><span>Verdant<small>Estate operations</small></span></Link><span className="eyebrow">Secure estate access</span><h1>Welcome back.</h1><p>Sign in with the email and password issued by your estate administrator.</p><form onSubmit={submit} className="login-form"><label>Email address<input required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" /></label><label>Password<input required type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Your password" /></label><p className="login-help" style={{marginTop:'-.4rem',textAlign:'right'}}><Link href="/forgot-password">Forgot password?</Link></p>{error&&<p className="form-error" role="alert">{error}</p>}<button className="action" disabled={loading}>{loading?'Signing in…':'Sign in'} <span aria-hidden="true">→</span></button></form><p className="login-help">Residents, administrators and gate staff use the same secure sign-in. Your account automatically opens the right workspace.</p></section><aside className="login-aside"><span className="eyebrow">One estate, clear roles</span><h2>The right view for every day.</h2><div className="role-list"><p><strong>Residents</strong>Balances, payments and account details.</p><p><strong>Administrators</strong>Estate-wide resident, billing and access operations.</p><p><strong>Gate staff</strong>Focused access activity for a confident gate.</p></div></aside></main> }
