import { createClient } from '@/lib/supabase/server'
import { requireRole } from '@/lib/auth'
import { PayButton } from '@/components/pay-button'
import { AdvancePaymentPanel } from '@/components/advance-payment-panel'
import Link from 'next/link'
export const dynamic = 'force-dynamic'
const naira=(n:number)=>`₦${n.toLocaleString()}`
type Resident={id:string;full_name:string;phone:string|null;email:string|null;relationship:string|null;house_id:string|null;houses:{address:string;house_type:string|null;billing_responsible_resident_id:string|null}|null}; type Invoice={id:string;period_label:string|null;amount:number;amount_paid:number|null;status:string;due_date:string|null;due_types:{name:string}|null}
export default async function Portal() { const user=await requireRole(['resident']);const supabase=await createClient();const {data,error:profileError}=await supabase.from('residents').select('id, full_name, phone, email, relationship, house_id, houses:houses!residents_house_id_fkey ( address, house_type, billing_responsible_resident_id )').eq('auth_user_id',user.id).single();const resident=data as unknown as Resident|null;
  if(profileError || !resident) return <div className="portal-wrap"><h1 className="page-title">Unable to load your resident profile</h1><p>Please reload or ask the estate office to check your account and database setup.</p></div>;

  const responsibleId = resident?.houses?.billing_responsible_resident_id ?? null
  const canSeeBilling = Boolean(resident) && (resident!.relationship === 'owner' || responsibleId === resident!.id)
  const isReassignedResponsible = Boolean(resident) && responsibleId === resident!.id && resident!.relationship !== 'owner'

  const {data:rows,error:invoiceError}=canSeeBilling&&resident?.house_id?await supabase.from('invoices').select('id, period_label, amount, amount_paid, status, due_date, due_types ( name )').eq('house_id',resident.house_id).order('due_date',{ascending:true}):{data:[],error:null};const invoices=(rows??[]) as unknown as Invoice[];const outstanding=invoices.reduce((sum,i)=>sum+Math.max(0,Number(i.amount)-Number(i.amount_paid??0)),0);const {data:fixedDueTypes}=canSeeBilling?await supabase.from('due_types').select('id, name, amount').in('name',['Service Charge','CDA Levy']):{data:[]};const outstandingInvoices=invoices.filter(i=>Number(i.amount)>Number(i.amount_paid??0));

  return <div className="portal-wrap"><section className="portal-welcome"><span className="eyebrow">Your home at Sample estate</span><h1>Hello, {resident?.full_name?.split(' ')[0]??user.name.split(' ')[0]}.</h1><p>Keep an eye on your estate account, charges and home details in one place.</p></section>{isReassignedResponsible&&<section style={{background:'#e7f5e9',border:'1px solid #bfe0c9',borderRadius:'.6rem',padding:'.85rem 1.1rem',marginBottom:'1.25rem',fontSize:'.85rem',color:'#276d4b',fontWeight:600}}>You&apos;re currently responsible for this house&apos;s estate bills.</section>}<section className="portal-stats">{canSeeBilling&&<article><span>Outstanding balance</span><strong className={outstanding?'red':''}>{naira(outstanding)}</strong><small>{outstanding?'Across current charges':'Your account is clear'}</small></article>}<article><span>Your residence</span><strong>{resident?.houses?.address??'Home record pending'}</strong><small>{resident?.houses?.house_type??'Sample estate'}</small><Link href="/portal/qr-pass" style={{display:'block',marginTop:'.6rem',fontSize:'.8rem',fontWeight:800,color:'var(--moss)'}}>View my gate pass →</Link></article></section>{canSeeBilling?<><section className="portal-grid"><article className="panel"><div className="panel-head"><h2>Estate charges{outstandingInvoices.length>0?` (${outstandingInvoices.length})`:''}</h2><Link href="/portal/payments" className="eyebrow">Payment history →</Link></div><div className="invoice-list">{invoiceError?<p className="empty" role="alert">Unable to load bills. Please reload or contact the estate office.</p>:outstandingInvoices.length?outstandingInvoices.map(i=><div className="invoice-row" key={i.id}><div><strong>{i.due_types?.name??'Estate charge'}</strong><span>{i.period_label??'Current period'}{i.due_date?` · Due ${new Date(i.due_date).toLocaleDateString()}`:''}</span></div><span className="pill">{i.status}</span><strong>{naira(Math.max(0,Number(i.amount)-Number(i.amount_paid??0)))}</strong><PayButton invoiceId={i.id} outstanding={Math.max(0,Number(i.amount)-Number(i.amount_paid??0))} /></div>):<p className="empty">Nothing outstanding — you&apos;re all caught up. See <Link href="/portal/payments">payment history</Link> for past charges.</p>}</div></article><article className="profile-card"><span className="eyebrow">Profile</span><h2>Your household details</h2><dl><div><dt>Email</dt><dd>{resident?.email??user.email??'Not provided'}</dd></div><div><dt>Phone</dt><dd>{resident?.phone??'Not provided'}</dd></div><div><dt>Relationship</dt><dd className="capitalize">{resident?.relationship??'Resident'}</dd></div></dl><p>Need to update a household detail? Contact the estate office.</p></article></section><section style={{marginTop:'1.5rem'}}><AdvancePaymentPanel dueTypes={fixedDueTypes??[]} /></section></>:<section className="profile-card" style={{marginTop:0}}><span className="eyebrow">Profile</span><h2>Your household details</h2><dl><div><dt>Email</dt><dd>{resident?.email??user.email??'Not provided'}</dd></div><div><dt>Phone</dt><dd>{resident?.phone??'Not provided'}</dd></div><div><dt>Relationship</dt><dd className="capitalize">{resident?.relationship??'Resident'}</dd></div></dl><p style={{marginTop:'.75rem'}}>Estate charges for this house are managed by the Home Owner (or whoever the estate office has designated). Use your gate pass above for entry and exit — contact the estate office for anything else.</p></section>}</div> }

export const metadata = {title: 'Portal', description: 'Manage your estate account and workspace with Verdant.', robots: {index: false, follow: false}}
